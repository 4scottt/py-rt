"""The web layer: the request context, the shell's templates and the routes.

Each later package brings its own router module and templates directory
entry; :mod:`pyrt.app` is where a router is registered.
"""
