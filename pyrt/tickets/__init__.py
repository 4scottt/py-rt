"""tickets: create, display and the history (plan §8, §11; FP T01-T04, T11-T14).

``service`` holds the queries and the writes, ``transactions`` the audit
record every write goes through, ``hooks`` the seam the mail package hangs
its notifications on, ``render`` the message body renderer and ``router``
the three screens.
"""
