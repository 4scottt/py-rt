"""customfields: the fields a queue's tickets carry (plan §8; FP F01-F07).

``service`` holds the queries and the writes — the admin pages' rows, plan
§12's fourth query for the ticket page, and the ``CustomField`` transaction
of plan §10 — and ``router`` the five screens plus the ``cf-{id}`` form
dependency the ticket pages read their values with.
"""
